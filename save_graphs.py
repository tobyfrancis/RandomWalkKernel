
def disorientation_adjacency_list(featureid,quats,neighborlist,surfacelist,cutoff):
    adjacency_list = []
    add_adjacency(adjacency_list,featureid,featureid_2,quat,surfacelist,cutoff)

    return adjacency_list
def save_graph(featureids,dataset,cutoff=False,experimental):
    try:
        for featureid in featureids:
            
    except IndexError:
    if experimental:
        #notate the graph
    else:
